{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:all": "pnpm exec playwright test",
    "test:regression": "pnpm exec playwright test --grep=@regression",
    "test:sanity": "pnpm exec playwright test --grep=@sanity",
    "test:smoke": "pnpm exec playwright test --grep=@smoke"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5"
  }
}
